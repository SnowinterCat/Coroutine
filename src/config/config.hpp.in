#pragma once
#include <coroutine/__config/os.hpp>
#include <coroutine/__config/compiler.hpp>
#include <coroutine/__config/platform.hpp>

#define SWC_CORO_VERSION                                                                           \
    ((((uint32_t)(SWC_CORO_VERSION_BUILD)) << 29U) |                                               \
     (((uint32_t)(SWC_CORO_VERSION_MAJOR)) << 22U) |                                               \
     (((uint32_t)(SWC_CORO_VERSION_MINOR)) << 12U) | ((uint32_t)(SWC_CORO_VERSION_ALTER)))
#define SWC_CORO_VERSION_STR "${VERSION}.${VERSION_BUILD}"
#define SWC_CORO_VERSION_MAJOR ${VERSION_MAJOR}
#define SWC_CORO_VERSION_MINOR ${VERSION_MINOR}
#define SWC_CORO_VERSION_ALTER ${VERSION_ALTER}
#define SWC_CORO_VERSION_BUILD ${VERSION_BUILD}
#define SWC_CORO_PLAT "${PLAT}"
#define SWC_CORO_ARCH "${ARCH}"
${define SWC_CORO_SHARED_BUILD}
#define SWC_CORO_MODE "${MODE}"
#define SWC_CORO_DEBUG ${DEBUG}
#define SWC_CORO_OS "${OS}"

#define SWC_CORO_BEGIN                                                                             \
    namespace coro                                                                                 \
    {
#define SWC_CORO_END }

#define AUTHOR_NAMESPACE_BEGIN                                                                     \
    namespace swc                                                                                  \
    {
#define AUTHOR_NAMESPACE_END }
